/**
 * @file my_stb_image.c
 * @brief stb_image / stb_image_write declarations used by the myui image
 * backend.
 *
 * Implementations live in engine/external/stb_image_impl.c and
 * engine/external/stb_image_write_impl.c and are linked through the shared
 * stb_image / stb_image_write targets.
 */
#include <stb_image.h>
#include <stb_image_write.h>
