# SDD ledger — plan set: 2026-08-29 rule engine full parity (4 sub-projects)

Setup notes:
- Spec: docs/superpowers/specs/2026-08-29-rule-engine-full-parity-design.md
- Prior delivery (bounded slices) landed via 2026-08-28 plan; its ledger is in
  .superpowers/sdd/2026-08-28-rule-engine-completion/progress.md
- Same standing rules: per-subproject commits, push after gate, 21 pre-existing dirty
  files + .omo/ never touched, review per task, ledger survives compaction.
- BASE at start: 4110f83 (post-merge).

## Sub-project A — GRL/expression surface (COMPLETE, committed bf11de3)
## Sub-project B — RETE/TMS/unification (pending)
## Sub-project C — streaming completion (pending)
## Sub-project D — ecosystem (pending)
Sub-project A plan committed: docs/superpowers/plans/2026-08-29-rule-engine-full-parity-a-grl.md
Upstream semantics research (agent-56) grounded: quantifiers !/exists/forall with prefix-heuristic candidates; no condition-method dispatch upstream (keep our stricter behavior, D2); two builtin sets; GRL query block grammar + executor semantics; accumulate prefix-scan semantics; full syntax inventory incl. aliases/multifield/test/action builtins.
A1: complete (uncommitted, review clean — spec OK, Approved; 7 minors deferred)
A1: minor (carried to A10): add tests for not_contains on non-string operands, relational rejection of bool/null; note NONE==NONE equality drift, strtod hex/inf acceptance, % int64-inexactness >2^53 as documented bounds
A2: fix round 1/5 (3 addressed, 0 open — I1 backward nested-quantifier honest NOT_SUPPORTED, I2 per-candidate NOT_FOUND absorb, M1 operator-stack grow token-drop family fix)
A2: complete (uncommitted, re-review clean)
A2: minor (carried to A10 docs): undotted leftmost fact ref in exists/forall takes plain-store fallback (upstream iterates) — bounded divergence to document; restricted-form quantifiers in backward keep legacy operand comparison
A3: complete (uncommitted) - condition built-ins len/length/size, isEmpty/is_empty, contains, exists/notExists/not_exists (builtins.c, registry-first fallback in ir_eval.c, exists() function-vs-quantifier disambiguation, arg-miss absorb -> false / true for notExists); surface 36 -> 48 tests; gate 17/17; ASan clean
A3: complete (uncommitted, review clean — spec OK, Approved; 5 minors deferred: exists((A==1)) undocumented divergence to A10 docs; magic-kind-2 naming; unknown-function skip-vs-false deviation documented; comment nit at parser.c:696; contract said 16 tests, actual 12)
A3: carry to B: backward chaining doesn't consult built-ins (backward parity gap list grows: nested quantifiers now NOT_SUPPORTED-honest; builtins unevaluated)
A4: complete (uncommitted) - action/RHS utility built-ins in builtins.c (log/print/println->stdout+message, now/timestamp string, deterministic xorshift64 random on engine->random_state fixed seed, format/sprintf %d%s%f%% + upstream {n}, count alias into A3 len, sum/add/max/min/avg INT64-preserving folds, round/floor/ceil/abs, contains/includes shared, startswith/endswith typed predicates, lowercase/uppercase/trim coercing, split->upstream debug string (no ABI change), join delim-first); re_builtin_call now takes engine+scratch, ir_eval scratch became re_eval_scratch_t; surface 48 -> 71 tests; gate 17/17; ASan clean
A4: fix round 1 (1 critical + 1 minor closed) - snprintf would-be-length clamp via sb_append_snprintf at all 5 sites (C1 %f 1e300 stack over-read); purity-classification caveat sentence at builtins.c + test comment (M1); regression test format("%f", 1e300-via-fact); surface 72/72; gate 17/17; ASan clean
A3: complete (uncommitted, review clean — Approved; minors to A10)
A4: fix round 1/5 (2 addressed, 0 open — C1 snprintf %f stack over-read clamped via sb_append_snprintf all sites; M1 purity mechanism comments)
A4: complete (uncommitted, re-review clean)
A4: minor (carried to A10): upstream-only aliases (maximum/ceiling/begins_with/strip/update/refresh) skipped — cheap follow-up; split Rust-Debug fidelity for control chars undocumented; function-in-condition first-pass-only behavior untested
A5: complete (uncommitted, review clean — Approved; 4 minors to A10: count==3.0 strict-equality lock test, flat-vs-structured conflict comment, exists(empty) regression comment, ledger timing)
A6: complete (uncommitted, review clean — Approved; minors to A10: accumulate purity observability test, cross-run stability test, exists(accumulate) divergence code comment)
A7: complete (uncommitted) - GRL query blocks: parser top-level query form (field-based, ';'-terminated, raw goal text, when via condition parser, bounded actions), IR query tables + validation, new query_exec.c on the Phase-3 bounded machine (textual &&/|| split, != direct fact eval, strategy/depth/solutions pass-through, memoization toggle -> disable_shared_proof_graph, optimization accepted no-op, on-missing folded into on-failure), re_engine_run_queries/re_engine_run_query appended to rule_engine.h; new suite test_rule_engine_query_blocks 24/24; gate 18/18; full 76/76; ASan+UBSan clean
A6: complete (Approved; minors to A10)
A7: implemented (uncommitted; 24 new tests in test_rule_engine_query_blocks.c; gate 18/18; concerns: strategy observability via proof-graph key, when-gate uses word ops while goal uses &&/||, on-missing folded into on-failure)
A7: complete (uncommitted, review clean — Approved; minors to A10: header doc for hard-error-no-dispatch + test, duplicate-name first-match doc, empty-name leniency, assignment-before-call upstream order note, X!=empty-side mapping)
A8: complete (uncommitted) - action surface: retract($Obj) -> `_retracted_<root>` flag fact + condition-read gating in ir_eval.c (gate_retracts; conditions only, reads-as-absent/NOT_FOUND), log(...) via A4 builtin machinery to stdout, ActivateAgendaGroup replaces program agenda focus for the run (+ new pop-time focus gate drops stale pending activations), ScheduleRule/CompleteWorkflow/SetWorkflowData dispatch to registered function of bare action name else NOT_SUPPORTED (D5); parser: whitelisted bare name(args) then-statements + bare $Ident fact reference; new RE_IR_ACTION_BUILTIN_CALL (append-only internal enum); surface 101 -> 111 tests; gate 18/18; ASan 18/18 clean; report: task-a8-report.md
A8: fix round 1 (review I1, Important, closed) - retract-specific pop-time hole: token-live pure entries now re-pass the gated linear re-match before firing (ordinary writes never had the hole - the network kills their token; the _retracted_ flag matches no pattern); regression test retract_gates_pending_rete_token_activation (red/green proven); surface 111 -> 112; gate 18/18; ASan 18/18 clean; report rationale sentence corrected
A8: fix round 1/5 (1 addressed, 0 open — retract-specific pop-time hole closed: token-live pure entries re-pass gated linear match; regression test red/green proven by re-reviewer)
A8: complete (uncommitted, re-review clean)
A8: minor (deferred): retract gate requires exactly BOOL true flag; string-literal builtin args ungated; O(facts) retract scan per read
A9: complete (uncommitted) - test(f(args)) CE (RE_EXPR_TEST; truthiness to_bool per types.rs L106; lone-function-call inner, rest parse error) + typed $x: Type(conds) (RE_EXPR_TYPED; exists-semantics over A2 prefix candidates with explicit type prefix; $x/bare-dotless-field rewrite to Type-rooted paths scoped to the form, outside use stays parse error); purity: test impure, typed pure-iff-inner; RETE-ineligible via collect(); backward NOT_SUPPORTED at both A2 gates; surface 112 -> 129 tests; gate 18/18; full build-gate 76/76; ASan 18/18 clean; report: task-a9-report.md
A9: fix round 1 (1 critical + 2 minors closed) - C1: typed-form dispatch gated on typed_form_ahead (`$ident :` lookahead) so $x.field works in atom-LHS position via the operand-path scope rewrite; compound test reworked to $o.-LHS coverage + new missing-member negative test; M1 exists($o: ...) lone-content boundary documented; M3 rewrite-site comment corrected; surface 129 -> 130; gate 18/18; ASan 130/130 clean; live probes $o: Order($o.amount > 100) fire/no-fire verified
A9: fix round 1/5 (3 addressed, 0 open — C1 typed-form LHS dispatch lookahead, M1/M3 comments)
A9: complete (uncommitted, re-review clean incl. independent live probes)
A10: complete - syntax sweep dispositions (/* */ comments, enabled attr, object literals, index syntax all upstream-absent parse errors, locked by syntax_sweep_unsupported_constructs_parse_error); carried minors folded (A1 not_contains/bool-null relational tests, A5 count==3.0 lock + 2 comments, A6 executor/cross-run tests + exists(accumulate) comment, A7 header doc + hard-error-no-dispatch test, A8 BOOL-true doc sentence, A3/A4 doc notes); 5 docs updated (conformance 9 new verified_local rows + reworked forall gap row, upstream expression/syntax-families/examples promoted, Design GRL-surface section, Architecture bullets, Implementation_Status 2026-08-29 entry); grl_surface 136/136, query_blocks 25/25, gate 18/18, full headless 75/75 (test_async_loader flaked once in parallel, passed standalone - known pre-existing), ASan clean; committed bf11de3 (+backward_ext.c beyond the literal add list - it carries A2 I1 tests; the 21-pre-existing check requires it); NOT pushed; report: task-a10-report.md
Sub-project A (A1-A10): COMPLETE — commit bf11de3, pushed via merge d537c7d (remote moved twice during the phase; second merge resolved an ir.h conflict: remote de-typedef'd re_ir_program_t, we kept our query tables + their struct form)
B1: fix round 1/5 (1 addressed, 0 open — justification_remove empty-rule guard) + Minor-3 pins folded
B1: complete (uncommitted, re-review clean — re-reviewer re-ran 18/18 + 19/19)
B2: resumed post-compaction — prior implementer's tree work found functionally complete (premise capture + per-premise re-validation + stats_v2 + 6 ported upstream cases); fresh implementer agent-0 audited vs brief, completed verification: focused 18/18, full headless 76/76 effective (test_async_loader known pre-existing parallel flake, passed standalone), ASan 18/18 clean; report: task-b2-report.md
B2: complete (uncommitted, review clean — spec OK, Approved; 0 Critical/Important) + controller-resolved items: CRLF/mixed endings from B1/B2 helper-script edits normalized to LF (HEAD convention; rebuild + 18/18 re-verified); rule_engine.h explicit-support doc hunks confirmed covered by B1 review package; backward_machine store-without-capture path verified clean by reviewer
B2: minor (deferred to final review): object/array facts fingerprint by type tag only (future member-level reads via re_facts_get_path must be captured or flip opaque — report documents); re_proof_graph_node_t.valid is upstream shape-fidelity, unconsumed by production code; dead overflow guard at proof_graph.c:511 (count capped at 32)
B3: complete (uncommitted, review clean — spec OK, Approved; focused 18/18 incl. ext suite 51/51 with 6 new tests TDD red→green, full 76/76, ASan 18/18; report: task-b3-report.md) + controller-resolved: clone-bindings-on-cache-hit lives in B2-reviewed code (goal() machine path); direct ?var == path never enters the shared proof graph (subscribe-only, verified by reviewer)
B3: minor (deferred to final review): direct-goal literal surface int64-only vs goal() actuals strtod doubles (consistent with pre-existing == path; one doc line would help); report's cache paragraph overstates proof-graph involvement for direct ?var goals (report-only, no code change); escaped quotes in goal() string actuals mis-tokenize (bounded surface; comment-worthy)
B4: complete (uncommitted, review clean — spec OK, Approved; focused 18/18 incl. agenda suite 45/45 with 8 new tests, full 76/76, ASan 18/18; report: task-b4-report.md) + controller-ratified divergences: NULL no-focus state never stacked (preserves ratified A8 grl_surface test; upstream MAIN-return documented as bounded divergence — B5 conformance docs must record); cross-group pending activations drop via A8 stale gate + re-push on pop-back (same net firings); line endings: B4-touched files normalized to LF (HEAD blob convention; autocrlf=true would normalize on commit anyway), rebuild + 18/18 re-verified
B4: minor (deferred to final review): focus-stack overflow cap (32) documented but not unit-tested (reviewer suggested ~5-line primitives-test loop pushing 33 groups); auto-focus gate bypass makes out-of-focus pure auto-focus rules re-evaluate every recompute cycle (mirrors upstream, documented — whole-branch reviewer visibility)
B5(steps 1-3): complete (uncommitted, review clean — spec OK, Approved; docs: 5 files, both known_gaps rows unsupported->tested with bounded_behavior parity, 4 new conformance rows, vapor documented file-level no invented lines; gate: clang Debug 76/76, ASan+UBSan 18/18 (build-rule-fresh-asan carries both flags; build-ci-ubsan UBSan flag inert under MSVC), MSVC build-rule-debug 18/18, bench regression PASS 0.377s vs 2.0s, git diff --check clean; report: task-b5-report.md)
B5: minor (deferred to final review): backward-qvar-unification row omits extension_abi sibling idiom; not_applicable is a one-off local_status vocabulary value; Architecture.md one 91-char reflow line; date split 2026-08-29 (plan-set idiom) vs 2026-08-30 (gate date) — consistent within each file
Final whole-branch review: With fixes — 2 Important (legacy zero-arg goal() unwrap mangles trailing-string-actual calls, proven live; docs net-firings claim unscoped for impure rules), 12 deferred minors triaged ALL SAFE-TO-LEAVE (rulings in review), strengths: B2 capture closed-world verified, ABI discipline exact, docs honesty mechanically verified
Final fix wave: complete (uncommitted) — backward.c:1466-1470 memchr interior-quote guard + 3 regression tests (red→green proven; ext suite 51->54); scoping clause in Implementation_Status.md/Rule_Engine_Design.md/rule_engine_conformance.yml; gate 18/18, ASan 18/18, diff --check clean; report: final-fix-report.md
Final re-review: ALL findings ADDRESSED, no new breakage — boundary arithmetic verified exact, zero-arg unwrap preserved, NOT composition both directions asserted
Sub-project B (B1-B5): COMPLETE — pending selective commit + push (B5 step 4)
B5 step 4: committed e2cff59 (18 files, +2768/-175, selective — 22 pre-existing dirty files + .omo/ untouched); remote moved (behind 4: 76871ec network ports, 16d3d7d math lib, cb965e0, 2ee2ab5 dep matrix) — merged clean (625265d), post-merge focused 18/18 + full headless 75/76 (test_network 3 UDP send failures from remote commit 76871ec, byte-identical to origin/master, environment-side, reproducible; B-phase files unaffected); pushed 2ee2ab5..625265d
Sub-project B (B1-B5): DELIVERED — commit e2cff59 + merge 625265d, pushed to origin/master
C1: complete (uncommitted, review clean — spec OK, Approved; stream_ext 20/20 with 8 new tests TDD red-first, focused 18/18, full 75/76 (test_network env), ASan 18/18, MSVC 18/18; report: task-c1-report.md) + controller-ratified scope: 3 mechanical one-liners in test_rule_engine_agenda.c (ABI pin 3u->4u), test_rule_engine.c + rule_engine_c99_consumer.c (initializer completion for -Werror) — C6 commit must include them; bitwise-double equality decision confirmed (reviewer ⚠️ resolved: dispatch ratified it)
C1: minor (deferred to final review): stream_value_equal comment should cite re_value_equal_typed (re_internal.h:708, == doubles) as nearer precedent vs TU-local value_equal; dead UINT64_MAX distinct guard; NaN under PERCENTILE sort unspecified-order comment (upstream panics — local non-crash mapping reasonable)
C2: complete (uncommitted, review clean after fix — spec OK; stream_ext 28/28 with 8 new tests, focused 18/18, full 75/76 (test_network env), ASan 18/18, MSVC 18/18; report: task-c2-report.md)
C2: fix round 1/5 (1 addressed, 0 open — CRLF test file converted to LF, content integrity verified via RUN_TEST parity + suite rerun; root cause: Git Bash grep CR-check pitfall, C1 reviewer also fooled — use file/tr for EOL checks)
C2: minor (deferred to final review): {NULL,nonzero} event_type identity-collapse comment inverts its mechanism (pathological-caller-only; fix = reject as INVALID_ARGUMENT or keep raw identity); odd-count trend half-split untested (matches upstream used/2 — pin with 3-window test); sample-stddev would pass current outlier thresholds (use 2.9 to discriminate); first/last STRING borrow-on-miss doc nit; percentile bitwise identity untested; engine/CMakeLists.txt is CRLF end-to-end (pre-existing build-file convention, out of LF rule scope)
C3: complete (uncommitted, review clean — spec OK, Approved; new suite stream_grl 19/19, focused 19/19, full 76/77 (test_network env), ASan 19/19, MSVC 19/19; report: task-c3-report.md) + controller-ratified scope: ir_validate.c (kind bound + payload branch — re_engine_install hard-rejects unvalidated IR); backward gate via RE_OPERAND_ARITHMETIC sentinel (C5 replaces with explicit wiring); && vs and disambiguation (join = and-of-two-stream-patterns)
C3: minor (deferred to final review): ir_validate stream-pattern branch looser than siblings (no size>0-when-has_type, no unset-sentinel pins — parser never produces it, cosmetic); report rule_engine.h citation drift (:257-261 vs actual :253-257); review-package snapshot bug (re_internal.h hunk missed — fixed by full re-baseline at snap-c3)
C4: complete (uncommitted, review clean — spec OK, Approved; stream_ext 45/45 with 17 new tests, focused 19/19, full 76/77 (test_network env), ASan 19/19, MSVC 19/19; report: task-c4-report.md) + controller-ratified: 3 test-file mechanical `, 0u` initializer completions (test_rule_engine.c, rule_engine_c99_consumer.c, rule_engine_fuzz_smoke.c — C6 commit must include all three); join `value` validated-not-retained (no payload surface in matches, documented); session-closure formula = record's own prospective session end (literal reading vacuous under single-session retention — reviewer verified, test-pinned); snapshot/restore + flag-off named risks resolved clean by reviewer
C4: minor (deferred to final review): join OOM paths not call-atomic (structurally safe; after transient OOM an unmatched event could re-emit — header note or decouple eviction from queue success); no outer-join SESSION_WINDOW eviction-branch test; expired-record-then-emit unpinned; extensions.c/tumbling_session.c whole-file CRLF->LF rewrites (pure EOL churn, convention-correct)
C5: complete (uncommitted, review clean — spec OK, Approved; new suite stream_eval 18/18, focused 20/20, full 77/78 (test_network env), ASan 20/20, MSVC 20/20; report: task-c5-report.md) + controller+reviewer ratified deviations: unregistered stream -> NOT_SUPPORTED (NOT_FOUND would be swallowed as silent non-match at engine.c:755 — verified); exists/single-activation CE semantics (upstream has no stream-CE eval to mirror); zero-duration tumbling -> INVALID_ARGUMENT; empty-tumbling bounds = bucket-0 span [0,retention) documented
C5: minor (deferred to final review): "timestamp order" doc overpromise for tumbling/session (append order — doc fix); session injection start uses unsorted events[0] (narrow late-ACCEPT edge); zero-duration tumbling INVALID_ARGUMENT unpinned; sliding exact-boundary inclusivity unpinned; session/empty-tumbling injection bounds untested; report RETE-pin overclaim (pin is C3's, cosmetic); test 12 weak second-run assertion; astronomical-timestamp WindowDurationMs saturation nit
C6(steps 1-3): complete (uncommitted, review clean — spec OK, Approved; docs: 5 files, full-streaming-patterns known_gaps unsupported->tested + 4 new verified_local rows + vapor row, upstream.yml 4 streaming rows with verified citations, Design/Architecture streaming sections, Implementation_Status dated entry; gate: full headless 77/78 (test_network env, async_loader flake standalone-pass), ASan+UBSan 20/20, MSVC 20/20, bench PASS 0.362s vs 2.0s, diff --check clean; suites 45/19/18/54/45/19; report: task-c6-report.md) + provenance: 4 docs drafted by first C6 dispatch (agent-25, provider-blocked mid-task), audited + completed by second (agent-26) — controller verified via mtimes
C6: minor (deferred to final review): stream-analytics row syntax field omits create call (cosmetic); Design.md executor-worker exclusion detail prose-only (C5 reviewer verified the code fact); Implementation_Status 76/78-raw vs 77/78-effective phrasing could be misread (cosmetic)
C final whole-branch review: With fixes — 1 Important (re_engine_stream_run signature wrong in conformance.yml:613 + Design.md:691 — name-based vs delivered (engine, facts, window)), 27 deferred minors triaged ALL SAFE-TO-LEAVE; cross-cutting risks all verified clean (B2 serial coherence, sentinel leakage, closure×analytics×injection, gating-idiom consistency, memory discipline, LF endings)
C final fix wave: complete (uncommitted) — both signature spots corrected; inverted identity-collapse comment at stream_analytics.c:159-162 corrected; join OOM exactly-once caveat at stream_join.c:10-13; yml re-parsed, focused 20/20, diff --check clean; report: c-final-fix-report.md
C final re-review: ALL findings ADDRESSED, no new breakage
Sub-project C (C1-C6): COMPLETE — pending selective commit + push (C6 step 4)
C6 step 4: committed 5e51e75 (27 files — 14 sources + 3 new sources + 7 tests + 2 new test suites + CMakeLists + 5 docs; 22 pre-existing dirty files + .omo/ untouched); remote unmoved (no merge needed); pushed 625265d..5e51e75
Sub-project C (C1-C6): DELIVERED — commit 5e51e75, pushed to origin/master
D1: complete (uncommitted, review clean after fix — spec OK; 16 pure plugin helpers as built-ins (concat/repeat/substring/replace, sqrt, first/last/reverse/slice/keys/values, isEmail/isPhone/isUrl/isNumeric/inRange), date family + 15-item vapor list documented-out; plugin_parity 33/33, focused 21/21, full 77/79, ASan 21/21, MSVC 21/21; report: task-d1-report.md) + ratified deviations: empty first/last -> Null (fetched upstream body overrode brief sketch), isUrl also accepts ftp:// (fetched body)
D1: fix round 1/5 (1 addressed, 0 open — replace empty-from now steps by UTF-8 codepoint via utf8_cp_len, upstream-exact claim now true; new pin replace("é","","-")=="-é-"; red->green proven by re-reviewer)
D1: minor (deferred to final review): nested structured members in debug rendering unpinned; vapor absent-pins incomplete (Modulo/Power/Ceil/Floor/AddHours/DateDiff/dayOfYear documented but not runtime-pinned absent); "1e999"->±inf overflow claim unpinned; builtins.c CRLF-on-disk report overgeneralization (neighbors are LF); strtod/ctype locale sensitivity theoretical; re_internal.h include in test (accepted); re_internal.h:852-860 roster comment stale -> D4 doc sweep
D2: complete (uncommitted, review clean after fix — spec OK; new suite example_coverage 3/3 smokes (ex01 fraud-style forward chain, ex03 action handlers, ex09 grl_query backward), focused 22/22, full 78/80, ASan 22/22, MSVC 22/22; report: task-d2-report.md) + premise corrections ratified: local module system EXISTS bounded (defmodule/import/export parser.c:1814-1826, visibility gating engine.c:721 — family 10 covered_bounded); 04-streaming family not a manifest family (session_window_demo.rs exists as auto-discovered root target outside the pinned 29-entry manifest — corrected in fix round)
D2: fix round 1/5 (2 addressed, 0 open — 04 entry now records session_window_demo existence accurately; medical_diagnosis pin attribution corrected to test_rule_engine.c:1932)
D2: carry to D4 docs: backward condition matcher reads FLAT fact names only (backward_operand_goal -> re_facts_get, no dotted object traversal; forward matcher walks) — bounded divergence documented in ex09 smoke; add to upstream.yml backward-queries row notes
D3: complete (no code changes; review Approved — all 5 evidence claims independently re-verified: Redis 8.10.1 MSYS2 LIVE at 127.0.0.1:6379 (off-PATH binaries at /d/soft/Redis-8.10.1-Windows-x64-msys2/, ~5.5-day uptime; controller's earlier no-service probe refuted), hiredis dev files ABSENT everywhere incl. VCPKG_ROOT without installed/ — the blocker is the client library, NOT the service; configure force-OFF verbatim at engine/CMakeLists.txt:736-753; boundary test OK + roundtrip SKIP compile-time keyed even against the live service; row stays optional_backend compile-verified per the spec exception; report: task-d3-report.md with verbatim evidence block for D4 citation)
D3: minor (deferred): dirty-set count in report said 22, actual 23 (LastTest.log counting ambiguity; set membership exact)
D4-carry: cite hiredis-absence (not "no service") as the Redis blocker + re-probe caveat; D2 flat-only backward condition matcher -> upstream.yml backward-queries row notes; D1 stale roster comment re_internal.h:852-860 -> deferred to D-final review (source file, outside D4 doc scope)
D4(steps 1-3): complete (uncommitted, review clean — spec OK, Approved; upstream.yml plugins row pending->tested + all-features CMake option-set mapping + examples per-family note + flat-only backward matcher divergence + D3 redis evidence (service live, hiredis blocker, re-probe caveat); conformance.yml new plugin-pure-helpers + example-family-coverage verified_local rows, baseline 22/22 + suite inventory; Design/Architecture/Implementation_Status entries; gate: full 79/80 (flake did not manifest, test_network env), focused 22/22 x3 trees, bench PASS 0.382s vs 2.0s, diff --check clean; report: task-d4-report.md)
D4: minor (deferred to final review): examples note specificity loss (grl_query_demo/templates old pins could be kept parenthetically); pre-existing YAML flow-style mangle on canonical-ir + grl-string-operators known_gaps rows (native-redis row fixed as side effect); Design.md "All delivered rows" vs Redis exception phrasing; stale CMake ref on default cargo-feature row (pre-existing); stale re_internal.h:852-860 roster comment
D final whole-branch review: READY TO MERGE = Yes — 0 Critical/Important; 15 deferred minors ALL SAFE-TO-LEAVE; cross-cutting verified clean (shape-based purity gates unbypassable by the 16 names, override-first dispatch, A3 arg-miss absorption, B/C machinery untouched, docs mechanically verified vs CMake/suites/gate report/live Redis re-probe); reviewer re-ran new suites 33/33 + 3/3 green
D final review recommendations parked (not merge blockers; kept out to preserve reviewed-state==committed-state): re_internal.h:852-860 roster comment + D1 family names (2 lines); upstream.yml:158 default-row stale CMake ref range (:704-742 -> :736-753)
D4 step 4: committed dff2d22 (9 files — builtins.c + CMakeLists + 2 new suites + 5 docs; 23 pre-existing dirty files + .omo/ untouched); remote unmoved; pushed 5e51e75..dff2d22
Sub-project D (D1-D4): DELIVERED — commit dff2d22, pushed to origin/master
PLAN SET COMPLETE: A (bf11de3+d537c7d), B (e2cff59+625265d), C (5e51e75), D (dff2d22) — all four sub-projects delivered, reviewed, gated, pushed
Residuals swept (user-directed): re_internal.h:852-860 roster comment + D1 family (16 names); upstream.yml:158 default-row local_ref -> the all-features option-set lines (:12-13, :74-75, :736-753, :789); yaml re-parsed, rebuild + focused 22/22 green; commit pushed
Residuals sweep (user-directed category-1): complete (uncommitted, review clean — 12/12 items verified per-item, Approved; focused 22/22 x3 trees, full 78/80 (2 pre-existing), suites agenda 45/45, stream_ext 48/48 (+3), stream_eval 22/22 (+4), plugin_parity 37/37 (+4), yaml whole-parse, diff --check clean; report: residuals-sweep-report.md) + ratified: overflow pin asserts discard-INCOMING keep-oldest-32 (long-documented semantic; brief wording was inverted); update/refresh vapor-listed (upstream handle_update_function is a no-op stub over vapor RETE-UL machinery)
Residuals sweep: minor (deferred): item-12 OOM branch coverage-dark (no allocation-failure injection harness); pre-existing backwards comment at test_rule_engine_stream_ext.c:1112 (strict-threshold wording); item-5a registers a sliding window against a tumbling clause (harmless, clause-driven rejection); newly-enumerated upstream aliases minimum/absolute/ends_with/tolower/toupper (engine.rs:1428-1438) remain unimplemented — one-line-each follow-up candidates
Residuals sweep committed 1ef38d8 (10 files, +445/-41) and pushed 72dbb30..1ef38d8; remote unmoved
Category-2 wave (user-directed): test_network root-caused (sendto to 0.0.0.0 wildcard — Winsock WSAEADDRNOTAVAIL; defect entered with 76871ec test edits; library innocent) + fixed via net_test_normalize_loopback at 3 sites; test_async_loader flake root-caused (fixed-iteration busy-loops as implicit ~10ms deadlines under CPU oversubscription — burner-proven 158/37 vs 0/100) + fixed via monotonic 5s deadline polls at 8 sites; Redis runtime-verified (tracked enablement fixes: winsock2 include + ws2_32 link; pristine hiredis v1.4.1 re-proof against live 8.10.1; conformance row promoted runtime-verified); review Approved (0 C/I); minors folded (::->::1 arm, doc line refs :736-759, Implementation_Status dated entry); committed 8e54306 + pushed
Housekeeping: 33 historical build-* trees deleted (1686MB -> 824MB; kept build-gate / build-rule-fresh-asan / build-rule-debug / build-redis-probe; CI references none); scratch dirs cleaned (diagnoses archived to test-network-diagnosis.md + test-async-loader-diagnosis.md); .scratch-hiredis kept for redis re-verification
Local WIP preserved (user-directed, nothing discarded): 21 pre-existing dirty files committed in 3 grouped commits (c141fa9 renderer/RHI, 6519085 myui, d908b9a core/platform incl. LastTest.log churn) after content review — all are MSVC-portability/shadowing-warning fixes, tree verified 80/80 green pre-commit; pushed 8e54306..d908b9a. Tool state (.omo/, .superpowers/, .scratch-hiredis/) kept on disk and excluded via .git/info/exclude (follows the .codegraph precedent). Working tree fully clean.
Follow-up wave (ledger optional items): 5 upstream aliases verified at f80a541 engine.rs:1428-1438 and wired in lockstep (dispatch + classifiers, ends_with the only new predicate) with parity/typing/error/override pins; completion_burst converted to 5s deadline (last implicit-deadline wait removed); review Approved 0 C/I; focused 22/22 x3 trees (controller re-verified), async_loader 17/17 incl. 64-burner contended run; committed + pushed

## 合卷 (2026-08-31)
Plan set closed. All four sub-projects delivered, reviewed, gated, pushed:
A bf11de3+d537c7d | B e2cff59+625265d | C 5e51e75 | D dff2d22
Post-set waves: residuals sweep 1ef38d8 + two-line fixes 72dbb30 | category-2 environment fixes + Redis runtime verification 8e54306 | local WIP preservation c141fa9+6519085+d908b9a | follow-up aliases + deadline conversion 32e0c5d
Final state: master = origin/master = 32e0c5d, working tree clean, full headless 80/80, focused 22/22 x3 toolchains, bench regression PASS.
Remaining content in this ledger = adjudicated design bounds (SAFE-TO-LEAVE, documented in conformance.yml/upstream.yml/Design.md) — not open work.
Archive: this ledger is committed to docs/superpowers/ledgers/2026-08-29-rule-engine-full-parity.md; the full workspace (task briefs/reports, review packages, diagnoses) stays on disk, excluded via .git/info/exclude.

## Whole-repo review wave (user-directed, 2026-09-01)
7 parallel subsystem reviewers over current HEAD 797c125 (rule_engine excluded — exhaustively reviewed): found 4 Critical + 17 Important + ~40 Minor. Fixed in 3 parallel waves (myui / renderer+asset / platform+core+physics) + rhi wave (recovered from a quota-blocked subagent that had completed the work) + a controller-diagnosed real race the wave itself surfaced: world_query ring slot claim was non-atomic (nested parallel-for made concurrent queries real); fixed via _Atomic + fetch_add claim — flaky test_ecs_system 1/20 -> 0/100 standalone + 0/100 under 16 burners.
Final whole-wave re-review (agent-77 + agent-86): every finding ADDRESSED with file:line evidence, no new Critical/Important; 2 new latent Minors ledgered (task_wait from a worker can live-lock — document contract; bvh degenerate-ray + negative max_dist corner). rhi_vk.c changes are review-verified only (ENGINE_VULKAN=OFF everywhere locally; needs one real Vulkan-enabled compile).
Gate: build-gate 81/81 (-LE graphics), build-rule-fresh-asan 82/82 (ASan+UBSan), build-rule-debug 82/82 (MSVC). Committed 2912562 (70 files, +2856/-333) + pushed. New suite test_indirect_draw (9 tests). Per-wave reports: repo-review-fix-*.md in this workspace.
Merge: remote moved 24 commits during the wave (text shaping/RTL, Vulkan vgcanvas AA, config-parser hardening, Redis source-tree build b1f1761). One content conflict (my_conf.c — remote had made the same ftell fix plus a size-budget check; theirs subsumed ours) resolved; remote's my_css.c broke the MSVC tree (C4456 shadow -> /WX error C2220) — fixed by reusing the outer loop var; remote's reworked Redis block absorbed our ws2_32 fix intact (applies to both hiredis paths now). Post-merge gates: build-gate 82/82, ASan 83/83, MSVC 83/83 (test count grew via remote suites).
Merge 2: one more remote commit (d81dae6 Wayland present history) merged clean (8537586); gates re-run green on all three trees (82/82, 83/83, 83/83); pushed d81dae6..8537586. Whole-repo review wave fully delivered.
Latent-minor follow-up (user-directed): task_wait/task_wait_handle now guard+document the worker-task self-deadlock contract (tls_worker_id early-return + task.h notes; regression test with never-hang design); bvh_raycast gained an explicit negative/NaN max_dist rejection — NOTE: the re-reviewer's "phantom hit at t=0" claim proved UNOBSERVABLE on red-check (the leaf's t < best_t filter already rejects; guard kept as explicit contract with honest comments); test NaN literal spelled (f32)NAN after MSVC C2124. Gates: 82/82 + 83/83 x2; test_task 0/50 loop. Committed f422925 + pushed.
Vulkan verification (user-directed): SDK 1.4.x headless-installed into engine/external/VulkanSDK/ (git-excluded via info/exclude); build-vk tree (clang, ENGINE_VULKAN=ON) — rhi_vk.c got its first real compile incl. the C-wave VK destroy fixes. Two blocking defects found+fixed: Windows VK branch never linked shaderc (suite never built on Windows before), and blind gpus[0] selection + unchecked surface-caps query (AMD iGPU driver 2.0.310 returns VK_ERROR_UNKNOWN for the caps query on this hybrid laptop; probe-isolated with a minimal harness) -> device probing now picks the presentable GPU (RTX 4060) + caps check fails cleanly (R574). Validation gate on real hardware: 0 validation messages. REMAINING: the grouped-indirect-compact compute path intermittently TDRs this NVIDIA driver (528.23, old) — vkQueueSubmit DEVICE_LOST cascade; when no TDR, everything else passes incl. golden images. Needs a current-driver re-run or the VK author's dev machine; not addressed (environment/driver-suspect, suite never ran on Windows before). Committed e6faff8 + pushed.
TDR investigation concluded (user-directed residual): bisect series (skip compact / skip upload / CB-ordered upload / probe-logged full runs) shows the wedge needs the visibility write present but is NOT explainable at the API level (0 validation messages; shader bounded 64 threads; buffers zero-init; host memcpy into HOST_VISIBLE mapping can no-op the GPU). Signature: intermittent — one run hung mid-frame (killed), several crashed through with DEVICE_LOST at TEST 10's frame_end submit, one run failed only the grouped-compact test while goldens passed, probe-logged run failed TEST 10/11/12 + goldens without hanging. Environment: hybrid laptop, NVIDIA driver 528.23 (Vulkan 1.3.224, mid-2023). Verdict: driver/environment-specific compute-path instability in the grouped-indirect-compact path — this suite never ran on Windows before e6faff8 unblocked it; needs a current NVIDIA driver re-run or the VK author's dev machine to separate driver vs engine; not fixed (would be blind). test_vulkan.c bisect instrumentation fully reverted; kept only the frame_end VkResult logging (this commit). build-vk tree + in-repo SDK remain for re-verification (Build_Guide documents the steps).
AMD enablement (user-directed: run on AMD, no NVIDIA driver install): probe4 proved ONLY the caps query fails on AMD 2.0.310 (formats 5 / present-modes 3 fine); vk_create_swapchain now falls back to safe caps (requested extent, identity transform, double buffer — R575) instead of crashing; RE_VK_DEVICE_INDEX=<n> env override pins a GPU. AMD run: suite completes, no TDR, TEST 10/11/12 PASS (TDR is NVIDIA-528.23-specific, confirmed res=-4 DEVICE_LOST); failures on AMD = golden image drift (MAE 28.75 vs tol 8 — golden refs are per-GPU, recorded on the author's machine) + 3 validation messages (2 = driver-echo of the broken caps query, 1 = REAL pre-existing spec violation: screenshot readback transitions a presentable swapchain image that is not currently acquired — timing-dependent, tolerated by other drivers; follow-up: restructure screenshot timing inside the frame). Default selection unchanged: probe prefers a fully-working GPU (NVIDIA here); AMD via RE_VK_DEVICE_INDEX=0.
NVIDIA driver retest (user installed 616.56, Vulkan 1.4.351): TDR PERSISTS (res=-4 DEVICE_LOST, wedge point wanders across runs: TEST 10 frame submit / TEST 9 init submits / early texture setup; driver reports no fault info via the new VK_EXT_device_fault forensics). AMD path remains fully functional (all tests pass; only per-GPU golden drift + 2 driver-echo messages). Conclusion: intermittent cross-GPU (Optimus dGPU->iGPU display copy) device-lost on this hybrid laptop — not an engine defect (AMD-solo run is functional-clean; validation gate 0 on both GPUs). Recommendation: verify on a desktop NVIDIA machine or dGPU-direct (MUX) mode. Tooling landed: VK_EXT_device_fault enablement + fault dump on the three device-lost failure sites (this commit) — future events print vendor fault info where the driver provides it.
Hybrid-topology compat round (user-directed): fixed the screenshot readback spec violation for real (transition of a presentable image outside the acquire window) — engine guard (legal window = frame_begin..present) + all capture sites moved to the post-frame_end/pre-present point (2 goldens, material-array, F12 deferred via g_screenshot_pending; the BREAK_SCREENSHOT hook was already legal); swapchain triple-buffers where legal (R577, less present back-pressure on hybrid); brace fix for the device-fault dump (was called unconditionally in init_image_layout — 10 spurious validation messages). Results: AMD run VALIDATION GATE 0, all functional tests pass (only per-GPU golden drift remains); NVIDIA hybrid path still loses the device at TEST 9 init submits (DEVICE_LOST -4, no fault info from driver) — verdict unchanged: Optimus cross-GPU topology on this laptop, not an engine defect; needs dGPU-direct/desktop verification. Gates: 82/82 + 83/83 + 83/83.
